# Environment Variables สำหรับ Kim Pai Repair Flow

# Application Settings
VITE_APP_NAME="Kim Pai Repair Flow"
VITE_APP_VERSION="1.0.0"

# API Configuration (ถ้ามี backend API)
# VITE_API_URL=https://your-api-url.com
# VITE_API_VERSION=v1

# Authentication (ถ้าใช้ external auth service)
# VITE_AUTH_DOMAIN=your-auth-domain.com
# VITE_AUTH_CLIENT_ID=your-client-id

# Feature Flags
VITE_ENABLE_MOCK_DATA=true
VITE_ENABLE_DEBUG=false

# Analytics (ถ้าใช้)
# VITE_GA_TRACKING_ID=GA-XXXXXXXXX
# VITE_HOTJAR_ID=your-hotjar-id

# Database (ถ้าใช้ client-side database)
# VITE_DB_URL=your-database-url

# File Upload (ถ้ามี)
# VITE_UPLOAD_URL=https://your-upload-service.com
# VITE_MAX_FILE_SIZE=10485760

# Notification Services (ถ้าใช้)
# VITE_NOTIFICATION_API_KEY=your-notification-key

# หมายเหตุ:
# - ตัวแปรทั้งหมดต้องขึ้นต้นด้วย VITE_ เพื่อให้ Vite สามารถใช้งานได้
# - ไม่ควรใส่ข้อมูลลับใน environment variables ที่ขึ้นต้นด้วย VITE_
# - สำหรับข้อมูลลับ ให้ใช้ server-side environment variables แทน